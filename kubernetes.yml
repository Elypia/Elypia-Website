# The configuration assumes a private configuration that looks like the following has been applied manually.
#
# apiVersion: v1
# kind: Secret
# metadata:
#   name: https
# data:
#   cert.pem:
#   key.pem:
#   ca.pem:

---
# Expose the Elypia Backend
# If using Cloud Hosting, this may create a new static IP
# on the host, make sure to update your DNS records to use it.
apiVersion: v1
kind: Service
metadata:
  name: elypia-website-service
  labels:
    app: elypia-website
spec:
  type: LoadBalancer
  selector:
    app: elypia-website
  ports:
    - name: http
      port: 80
      protocol: TCP
    - name: https
      port: 443
      protocol: TCP
---
# Deploy the Elypia Backend
apiVersion: apps/v1
kind: Deployment
metadata:
  name: elypia-website
spec:
  replicas: 1
  selector:
    matchLabels:
      app: elypia-website
  template:
    metadata:
      labels:
        app: elypia-website
    spec:
      volumes:
        - name: https
          secret:
            secretName: https
      containers:
        - name: elypia-website
          image: us.gcr.io/elypia/elypia-website:1.0.5
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
            - name: https
              containerPort: 443
              protocol: TCP
          volumeMounts:
            - name: https
              mountPath: /etc/nginx/certs/
          readinessProbe:
            httpGet:
              path: /index.html
              port: 80
            initialDelaySeconds: 15
            timeoutSeconds: 5
            periodSeconds: 15
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /index.html
              port: 80
            initialDelaySeconds: 15
            timeoutSeconds: 5
            periodSeconds: 15
            failureThreshold: 3
---
